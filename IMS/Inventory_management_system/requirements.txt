Django==5.1.15
django-auto-prefetch==1.14.0
dj-database-url==1.3.0
python-dotenv==1.2.2
openpyxl==3.1.5
Pillow==12.3.0
psycopg2-binary==2.9.11
django-crispy-forms==2.5
crispy-bootstrap5==2025.6

# REST API and Geospatial Support (Phase 2 - Ghana Map)
djangorestframework==3.15.2
matplotlib==3.10.8
pandas==2.2.3
reportlab==4.4.9
# HTML->PDF for release memo/letter (Ministry letterhead + styled templates).
# Needs system libs: libpango-1.0-0 libpangocairo-1.0-0 libgdk-pixbuf-2.0-0
# libcairo2 libffi-dev — installed by start.sh on boot (see start.sh).
weasyprint==69.0
seaborn==0.13.2
xlsxwriter
gunicorn==25.0.3
whitenoise==6.11.0
plotly==6.5.2

# Two-Factor Authentication
django-otp==1.4.0
pyotp==2.9.0
qrcode[pil]==8.2

# Microsoft 365 Integration
msal>=1.32.0
requests>=2.32.3
cryptography>=46.0.7

# Phase F.2 — signed-scan validation.
# Primary decoders (REQUIRED): pure-pip, ship as binary wheels — no system
# poppler / zbar needed. These work out of the box on Azure App Service.
opencv-python-headless>=4.8.0
pymupdf>=1.24.0
# qrcode (for generating QR codes during PDF generation) is already listed under 2FA above.
#
# Legacy decoders (kept as optional fallbacks; the validator skips them
# if they're not importable). pyzbar wants libzbar at the OS level and
# pdf2image wants poppler-utils — production no longer depends on either.
pyzbar>=0.1.9
pdf2image>=1.17.0
# Phase H — daily SQLite backup to Azure Blob Storage
azure-storage-blob>=12.19.0
# Media (user uploads) stored on Azure Blob via Django's storage API so files
# survive deploys/restarts and work across multiple App Service instances.
django-storages[azure]>=1.14.2

# =============================================================================
# Hardening (see HARDENING_PLAN.md)
# =============================================================================
# Brute-force login protection (admin / password auth)
django-axes==7.0.0
# Per-view rate limiting (OAuth login/callback, bulk uploads)
django-ratelimit==4.1.0
# Content-Security-Policy header
django-csp==4.0
# Error tracking (settings.py auto-enables when SENTRY_DSN is set)
sentry-sdk==2.20.0
# Redis client for the shared cache / rate-limit + lockout backend at scale
redis==5.2.1
# Postgres in production (psycopg2-binary already present above)
