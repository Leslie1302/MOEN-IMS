# Session Handoff — MOEN-IMS Gap-Closure Work
**Date:** 2026-07-17 · **For:** next Claude session (Opus)
**Read alongside:** `IMPLEMENTATION_PLAN_2026-07-16.md` (phase log, all decisions recorded)

## Where things stand

Phases 0–5 of the gap-closure plan are DONE, tested (199+ tests green locally), and pushed. Phase 6 (six user decisions, all answered today) is **implemented but NOT yet test-run** — that is the immediate next step.

## Immediate next step (do this first)

```bash
cd IMS/Inventory_management_system
python manage.py test Inventory
```

All Phase 6 code compiles but has not been executed. Expected risk areas if anything is red:

1. `services/waybill_pdf.py` — 1,240 lines moved out of `transporter_views.py` (pure relocation). ~~Risk~~ RESOLVED: the move initially clipped `TransportArchiveView.get_context_data`'s `return context` (suite caught it, 202/203); restored 2026-07-17. `transporter_views.py` re-exports `download_waybill_pdf`, `verify_waybill_qr` at the bottom so `urls.py` is unchanged.
2. `map_views.ghana_map_data_api` — headline now comes from `compute_access_rate()` (meter formula). If tests have no seeded `AccessRateConfig` (migration 0058 should seed one), the view falls back to `source='consultant_inputs_fallback'` — tests assert membership in both sources, so this should hold.
3. Migration `0069_remove_dead_order_statuses` — handwritten AlterField (choices only). Runs during test-DB creation; if the field kwargs drift from the model, `makemigrations --check` will complain. Fix by running `makemigrations` and comparing.

After green: push, then on prod run `python manage.py migrate` and (once) `python manage.py sync_community_sites`.

## Phase 6 decisions (user answered these explicitly — do not relitigate)

| # | Decision | Status |
|---|---|---|
| 1 | Delete 'Pending' + 'Ready for Pickup' statuses | DONE — model + migration 0069 |
| 2 | Project status derives from sites | DONE — `Project.derived_status`, used in project_list/detail templates |
| 3 | **Meter formula IS the methodology** (meters × persons-per-connection vs population) — wire it | DONE — map headline = `compute_access_rate()`, consultant signal alongside as `consultant_rate_pct` |
| 4 | Show all three signals (meters / consultant % / material delivery) | DONE — all in map payload |
| 5 | Off-inventory releases: warn-but-allow, surfaced in UI | DONE — `order.processing_warning` appended to both endpoints' JSON messages |
| 6 | Move waybill PDF to services; delete seed_simulation, keep seed_demo_data | DONE |

## Architecture decisions from this week (context for future work)

- **No microservices / no app splits.** User explicitly chose closing gaps in place. Modular boundaries via `services/` package.
- **Status is explicit** (Phase 3): `MaterialOrder.save()` no longer recomputes `status`; `services/order_flow.process_quantity()` is the ONLY processing path (signed-letter guard + stock deduction live there).
- **Delivered only via Site Receipt** (Phase 1): `update_transport_status` rejects 'Delivered'; receipts post BoQ drawdown.
- **Map populates from the Community registry** (Phase 4): `services/map_sync.ensure_site_for_community` + Community post_save signal + `sync_community_sites` command. BoQ↔registry mismatches shown on the community progress page.
- **Canonical groups**: 'Store Officers', 'Consultants' (migration 0068 merged aliases); checks go through `Inventory.utils.Roles`.
- Prod DB is **Azure Postgres** (not SQLite — that's only local dev).
- CI: `.github/workflows/tests.yml` runs full suite (merge gate); `dependency-scan.yml` blocks on pip-audit.

## Known loose ends (small, in rough priority order)

1. `test_meter_install_does_not_move_headline` in `test_map_view.py` — name/docstring now stale (meters DO move the headline post-decision-3); assertion (`assertGreaterEqual`) still passes. Rename when touched.
2. Dead-status references remain in dashboards/templates ('Pending' counts, emoji maps) — harmless (always zero), delete opportunistically.
3. `AccessRateConfig` values (persons-per-connection, baseline, total population) drive the new headline — user should verify the seeded numbers in admin match current census figures.
4. Audit leftovers not yet done: blanket `except Exception` cleanup (~100 sites), 'Consultants' vs `Roles.CONSULTANT` naming could be tightened further.
5. `brag-output/` directory at repo root is untracked clutter (from a plugin) — ask user before deleting.

## Working agreements with this user

- Ponytail (lazy/minimal) mode active — smallest working diff, no speculative abstractions, `ponytail:` comments mark deliberate ceilings.
- User runs tests locally and pastes output (Claude's sandbox cannot install the deps — pip times out; don't retry that).
- E2E test (`test_end_to_end_workflow.py`) is the gate for every change.
- Concise responses; user prefers doing over discussing.
